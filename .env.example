MYSQL_ROOT_PASSWORD=change_this_root_password
MYSQL_DATABASE=orangehrm
MYSQL_USER=orangehrm
MYSQL_PASSWORD=change_this_orangehrm_password

# Ports: OrangeHRM -> 8080, phpMyAdmin -> 8081 (change if needed)

# Notes:
# - After `docker compose up`, visit http://localhost:8080 to run OrangeHRM web installer.
#   In the installer, use DB host: `db`, port: `3306`, and the DB credentials above.
# - phpMyAdmin (optional) will be available at http://localhost:8081 (user: as set above).
# - Persisted data lives in Docker volumes `db_data` and `orangehrm_data` defined in compose.
